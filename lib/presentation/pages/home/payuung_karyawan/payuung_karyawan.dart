import 'package:flutter/material.dart';

class PayuungKaryawan extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Payuung Karyawan"),),
    );
  }
  
}